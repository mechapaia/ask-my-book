# Copy this file to `.env` and fill in your key.
# `.env` is git-ignored so your real key is never committed.

# Free API key from https://console.groq.com (used for answer generation in Week 2)
GROQ_API_KEY=
